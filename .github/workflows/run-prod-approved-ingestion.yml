# DEPRECATED for golden-path x5j9 (Epic #223 / #232).
# Use `.github/workflows/run-ingestion.yml` instead of this source-specific path.

name: "[deprecated] Run PROD approved CDC ingestion"

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: prod-approved-ingestion-deprecated
  cancel-in-progress: false

jobs:
  redirect:
    name: Redirect to generic run-ingestion
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed with migration guidance
        run: |
          echo "::error::Deprecated for cdc_lyme_x5j9_wybp. Use workflow run-ingestion.yml."
          exit 1
