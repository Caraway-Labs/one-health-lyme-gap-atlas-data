# DEPRECATED for golden-path x5j9 (Epic #223 / #232).
# Use `.github/workflows/run-ingestion.yml` with
# source_definition=config/sources/cdc_x5j9_wybp.yml instead.
# This workflow remains temporarily for rollback reference and will be removed
# after the measured velocity closeout; it is not the supported happy path.

name: "[deprecated] Capture PROD CDC evidence for steward review"

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: prod-cdc-evidence-capture-deprecated
  cancel-in-progress: false

jobs:
  redirect:
    name: Redirect to generic run-ingestion
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed with migration guidance
        run: |
          echo "::error::Deprecated for cdc_lyme_x5j9_wybp. Use workflow run-ingestion.yml (operation=run, tier=B|C)."
          exit 1
