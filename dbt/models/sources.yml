version: 2
sources:
  - name: raw
    database: "{{ target.database }}"
    schema: RAW
    tables:
      - name: cdc_lyme_qtbi_xd4i
        config:
          enabled: "{{ target.database in ['ONE_HEALTH_LYME_GAP_ATLAS_DEV', 'ONE_HEALTH_LYME_GAP_ATLAS_PROD'] }}"
      - name: cdc_lyme_x5j9_wybp
        columns:
          - name: data_source_version_id
            tests: [not_null]
          - name: source_row_hash
            tests: [not_null]
